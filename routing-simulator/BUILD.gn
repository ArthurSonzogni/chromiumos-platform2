# Copyright 2023 The ChromiumOS Authors
# Use of this source code is governed by a BSD-style license that can be
# found in the LICENSE file.

group("all") {
  deps = [ ":librouting-simulator" ]
  if (use.test) {
    deps += [ ":routing_simulator_testrunner" ]
  }
}

pkg_config("target_defaults") {
  cflags = [ "-Wconversion" ]
  pkg_deps = [
    "libchrome",
    "libnet-base",
  ]
}

static_library("librouting-simulator") {
  sources = [ "routing_table.cc" ]
  configs += [ ":target_defaults" ]
}

#TODO(b/302635355): Add an executable in this package.

if (use.test) {
  pkg_config("test_config") {
    pkg_deps = [ "libchrome-test" ]
  }
  executable("routing_simulator_testrunner") {
    run_test = true
    sources = [ "routing_table_test.cc" ]
    configs += [
      "//common-mk:test",
      ":target_defaults",
      ":test_config",
    ]
    deps = [
      ":librouting-simulator",
      "//common-mk/testrunner",
    ]
  }
}
