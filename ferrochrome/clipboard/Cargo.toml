[package]
name = "clipboard_sharing_server"
version = "0.1.0"
authors = ["The Chromium OS Authors"]
edition = "2021"

[dependencies]
anyhow = "1.0"
libchromeos = { path = "../../libchromeos-rs/" } # provided by ebuild
log = "0.4"
tokio = { version = "1.29", features = ["macros", "rt-multi-thread", "sync", "time"] }
vsock = "0.3"
