% minijail-config-file v0

# Raise ambient capabilities.
ambient

# Filesystem access rules.
fs-default-paths
fs-path-ro = /proc
fs-path-rw = /dev
fs-path-rw = /run/dbus
fs-path-rw = /run/udev
fs-path-rw = /sys

c = 0x300b
u = devbroker
i
n
